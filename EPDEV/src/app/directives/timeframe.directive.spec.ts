import { TimeframeDirective } from './timeframe.directive';

describe('TimeframeDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeframeDirective();
    expect(directive).toBeTruthy();
  });
});
